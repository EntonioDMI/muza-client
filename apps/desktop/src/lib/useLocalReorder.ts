/** Пенёк: хук переехал в @muza/app/lib/useLocalReorder (Э3 веб-паритета).
 *
 *  Это тот самый «адекватный реордер», которого в вебе не было вообще: плашка
 *  следует за курсором в пределах своей области, соседи разъезжаются. Жест
 *  pointer-only, платформы не касается — переехал как есть.
 *
 *  Файл существует, чтобы потребители `from "../lib/useLocalReorder"` не
 *  получили дифф в этапе, который к ним отношения не имеет. Новый код
 *  импортирует напрямую из "@muza/app/lib/useLocalReorder". */
export * from "@muza/app/lib/useLocalReorder";
