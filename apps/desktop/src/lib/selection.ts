/** Пенёк: математика выделения переехала в @muza/app (Э3 веб-паритета,
 *  2026-08-02) вместе с панелью очереди — она теперь общая для приложения и
 *  веба, а из общего пакета в приложение импортировать нельзя.
 *
 *  Файл остаётся, чтобы потребители (`from "./selection"`, lib/selection.test.ts)
 *  не получили дифф в этапе, который к ним отношения не имеет.
 *  Новый код импортирует из "@muza/app/lib/selection". */
export * from "@muza/app/lib/selection";
