/** Пенёк: рисунок шеринг-карточек переехал в @muza/app/lib/shareCard (волна
 *  экранов веб-паритета, 2026-08-02) вместе со своим единственным
 *  потребителем — диалогом «Поделиться».
 *
 *  Одно отличие общего модуля: глиф Muza приезжает ссылкой параметром, а не
 *  импортом картинки (сборщики двух площадок превращают такой импорт в
 *  разное). Ссылку подставляет обёртка shell/ShareDialog.tsx.
 *
 *  Файл существует, чтобы App.tsx (берёт отсюда тип ShareData) не получил
 *  дифф. Новый код импортирует из "@muza/app/lib/shareCard". */
export * from "@muza/app/lib/shareCard";
