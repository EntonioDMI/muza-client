/** Пенёк: реестр шрифтов переехал в @muza/app (`@muza/app/prefs/fonts`) —
 *  это набор допустимых значений полей Prefs.fontUi/fontDisplay, часть модели
 *  настроек, а не приложения (обоснование выбора папки — в шапке самого файла).
 *
 *  Файл существует, чтобы потребители `from "../lib/fonts"` не получили дифф
 *  в этапе, который к ним отношения не имеет (паттерн Э0, см. i18n). */
export * from "@muza/app/prefs/fonts";
