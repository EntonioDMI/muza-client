/** Пенёк: модуль переехал в @muza/app (`@muza/app/prefs/themes`) вместе с
 *  моделью настроек — классификация ключей оформления обязана лежать рядом с
 *  Prefs, иначе появляется вторая правда (сторож themes.coverage.test.ts).
 *
 *  Файл существует, чтобы потребители `from "../lib/themes"` не получили дифф
 *  в этапе, который к ним отношения не имеет (паттерн Э0, см. i18n). */
export * from "@muza/app/prefs/themes";
