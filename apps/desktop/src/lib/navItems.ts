/** Пенёк: компоновка сайдбара переехала в @muza/app (волна «настройки»,
 *  2026-08-02) вместе с под-экраном «Навигация» — под-экран теперь общий, а
 *  тянуть из общего пакета в приложение направление зависимостей запрещает.
 *
 *  Файл существует, чтобы потребители `from "../lib/navItems"`
 *  (shell/Sidebar.tsx) не получили дифф в этапе, который к ним отношения не
 *  имеет. Новый код импортирует из "@muza/app/lib/navItems". */
export * from "@muza/app/lib/navItems";
