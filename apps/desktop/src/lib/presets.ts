/** Пенёк: модуль переехал в @muza/app (`@muza/app/prefs/presets`) вместе с
 *  моделью настроек — наборы пресетов это значения полей Prefs.
 *
 *  Файл существует, чтобы потребители `from "../lib/presets"` не получили дифф
 *  в этапе, который к ним отношения не имеет (паттерн Э0, см. i18n). */
export * from "@muza/app/prefs/presets";
