/** Пенёк: модуль переехал в @muza/app/lib/dragEngine (Э3 веб-паритета).
 *
 *  Почему именно он уехал первым: это чистая математика жеста без единого
 *  импорта и без единого касания Tauri — на Pointer Events, а значит тот же код
 *  работает пальцем в браузере. Держать вторую копию в вебе значило бы
 *  разъехаться по порогам (HOLD_MS/DRAG_THRESHOLD), а их владелец уже дважды
 *  правил по живому ощущению.
 *
 *  Файл существует, чтобы потребители `from "../lib/dragEngine"` не получили
 *  дифф в этапе, который к ним отношения не имеет. Новый код импортирует
 *  напрямую из "@muza/app/lib/dragEngine". */
export * from "@muza/app/lib/dragEngine";
