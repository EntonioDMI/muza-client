/** ПЕНЁК форматирования для списков и плеера.
 *
 *  Тайм-код `fmtTime` переехал в @muza/app (Э3 веб-паритета, 2026-08-02):
 *  его показывает общая полоса плеера, и время в обеих программах обязано
 *  считаться одинаково (веб считал по-своему и расходился на секунду).
 *
 *  Имена источников (providerLabel/primarySourceLabel) уехали следом, в волну
 *  «экраны» (2026-08-02): бейдж «откуда возьмётся звук» рисует теперь общий
 *  экран поиска, и словарь провайдеров обязан быть один на оба клиента.
 *
 *  Файл остаётся ре-экспортом, чтобы полтора десятка потребителей
 *  `from "../lib/format"` (вьюхи чужих зон, диалог версий, «Заменить версию»)
 *  не получили дифф в этапе, который к ним отношения не имеет.
 *  Новый код берёт всё это из "@muza/app/lib/format". */

export { fmtTime, primarySourceLabel, providerLabel } from "@muza/app/lib/format";
