/** Пенёк: модуль переехал в @muza/app/lib/coverArt (Э3 веб-паритета).
 *
 *  Уехал вместе со слоем перетаскивания: превью под курсором рисует обложку и
 *  без кропа полей показывало бы кадр 4:3 с каймой. Веб до сих пор лечил ту же
 *  беду вторым способом (<Cover> из ДС) — теперь у обоих клиентов один путь.
 *
 *  Файл существует, чтобы потребители `from "../lib/coverArt"` не получили
 *  дифф в этапе, который к ним отношения не имеет. Новый код импортирует
 *  напрямую из "@muza/app/lib/coverArt". */
export * from "@muza/app/lib/coverArt";
