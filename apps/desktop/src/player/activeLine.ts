/** Пенёк: сама функция уехала в общий пакет (16.08), потому что у веба всё это
 *  время лежала её копия. Разбор и все числа — @muza/app/lib/activeLine. */

export { activeLyricLine, lyricSeekSec, LYRICS_LEAD_MS } from "@muza/app/lib/activeLine";
export type { ActiveLineOpts, TimedLine } from "@muza/app/lib/activeLine";
