/** Пенёк: хук переехал в @muza/app/lib/videoSync (Э7 веб-паритета) — его
 *  единственный потребитель, панель «Сейчас играет», стала общей, а сам хук —
 *  чистый React + <video>, ничего платформенного.
 *
 *  Файл существует ради useVideoSync.test.ts рядом (дрейф-математика, 6
 *  тестов): он импортирует "./useVideoSync" и через пенёк проверяет уже общий
 *  модуль. Сносится косметическим коммитом в конце рефактора. */
export * from "@muza/app/lib/videoSync";
