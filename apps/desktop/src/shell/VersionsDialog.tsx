/** Пенёк: разворот «Версии и источники» переехал в
 *  @muza/app/shell/VersionsDialog (волна экранов веб-паритета, 2026-08-02).
 *  Два его касания приложения — файл трека и разобранные источники в памяти
 *  плеера — сведены в порт розетки preparedTracks (см. общий файл и
 *  src/platform/desktopAdapter.ts): вилка приложения делает ровно то же, что
 *  делал диалог, поэтому поведение не изменилось.
 *
 *  Новый код импортирует из "@muza/app/shell/VersionsDialog". */
export { VersionsDialog } from "@muza/app/shell/VersionsDialog";
