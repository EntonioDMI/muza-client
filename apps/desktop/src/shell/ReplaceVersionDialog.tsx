/** Пенёк: диалог «Заменить версию» переехал в
 *  @muza/app/shell/ReplaceVersionDialog (волна экранов веб-паритета,
 *  2026-08-02) вместе со страницей плейлиста, откуда его открывают.
 *  Касаний площадки в нём не было — переезд дословный.
 *
 *  Новый код импортирует из "@muza/app/shell/ReplaceVersionDialog"
 *  (типы ReplaceCtx/ReplaceTarget — оттуда же). */
export { ReplaceVersionDialog } from "@muza/app/shell/ReplaceVersionDialog";
export type { ReplaceCtx, ReplaceTarget } from "@muza/app/shell/ReplaceVersionDialog";
