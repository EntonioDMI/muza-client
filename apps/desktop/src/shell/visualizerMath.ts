/** Пенёк: модуль переехал в @muza/app/lib/visualizerMath (Э7 веб-паритета) —
 *  визуализатор нужен обоим клиентам, а математика тут чистая (ни Tauri, ни
 *  DOM, ни canvas).
 *
 *  Файл существует, чтобы не трогать потребителей, к которым этап отношения не
 *  имеет: SettingsView (VIS_LIMITS), lib/visualizerPresets.test.ts и главное —
 *  visualizerMath.test.ts рядом, 388 строк тестов, которые через этот пенёк
 *  проверяют уже общий модуль. Сносится косметическим коммитом в конце
 *  рефактора вместе с остальными пеньками.
 *
 *  ⚠️ Подпуть именно "lib", а не "shell": в exports пакета "./shell/*" ведёт
 *  на *.tsx, а "./lib/*" — на *.ts. Общий package.json ради этого не правили. */
export * from "@muza/app/lib/visualizerMath";
