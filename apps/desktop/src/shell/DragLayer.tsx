/** Пенёк: слой переехал в @muza/app/shell/DragLayer (Э3 веб-паритета).
 *
 *  ⚠️ Пенёк обязан оставаться РЕ-ЭКСПОРТОМ, а не своей копией контекста: у
 *  DragLayer контекст модульного уровня, и вторая копия модуля означала бы
 *  второй React-контекст — вью, импортирующие useDrag отсюда, перестали бы
 *  видеть провайдер, поднятый оттуда (и наоборот). Пока это `export *`, обе
 *  записи ведут в один модуль, и провайдер один на всё дерево.
 *
 *  Новый код импортирует напрямую из "@muza/app/shell/DragLayer". */
export * from "@muza/app/shell/DragLayer";
