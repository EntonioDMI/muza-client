/** Пенёк: диалог «Поделиться плейлистом» переехал в
 *  @muza/app/shell/ShareVisibilityDialog (волна экранов веб-паритета,
 *  2026-08-02) вместе со страницей плейлиста, которая его открывает.
 *  Касаний площадки в нём не было — переезд дословный.
 *
 *  Файл существует, чтобы потребители (страница плейлиста приложения, App.tsx)
 *  не получили дифф. Новый код импортирует из
 *  "@muza/app/shell/ShareVisibilityDialog". */
export { ShareVisibilityDialog } from "@muza/app/shell/ShareVisibilityDialog";
