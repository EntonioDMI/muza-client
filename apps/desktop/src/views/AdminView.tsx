/** Пенёк: админка переехала в @muza/app (волна «экраны», 2026-08-02). Экран
 *  чистый React без единого обращения к Tauri — в браузере работает как есть,
 *  поэтому у веба теперь та же страница, а не отсутствие пункта.
 *
 *  Файл существует, чтобы `from "./views/AdminView"` в App.tsx не получил дифф
 *  в этапе, который к нему отношения не имеет. Новый код импортирует напрямую
 *  из "@muza/app/views/AdminView". */
export * from "@muza/app/views/AdminView";
