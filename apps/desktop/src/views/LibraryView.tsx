/** Пенёк: «Твоя медиатека» переехала в @muza/app (волна экранов веб-паритета,
 *  2026-08-02) — тот же экран теперь рисует и веб (/library).
 *
 *  Обёртки здесь нет намеренно: всё приложенческое, что было в экране, ушло в
 *  РОЗЕТКУ (packages/app/src/platform). Файлы с диска — порт localFiles
 *  (вилка приложения — src/platform/desktopAdapter.ts), вынос файла
 *  Alt+перетаскиванием — порт dragOut. Приложение отдаёт оба порта, поэтому
 *  вкладка «Локальные», «Показать в папке» и Alt+перетаскивание работают ровно
 *  как раньше; браузер портов не отдаёт — и этих возможностей там просто нет.
 *
 *  App.tsx зовёт LibraryView теми же пропами, что и до переезда.
 *  Новый код импортирует из "@muza/app/views/LibraryView". */
export { LibraryView } from "@muza/app/views/LibraryView";
