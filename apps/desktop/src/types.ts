/** Пенёк: МОДЕЛЬ НАСТРОЕК (Prefs, DEFAULT_PREFS и всё вокруг них) переехала в
 *  @muza/app (`@muza/app/prefs/types`) — экран настроек становится общим, и
 *  вторая копия Prefs завела бы две правды: поле, добавленное здесь, молча
 *  отсутствовало бы в браузере. Сторож переехал вместе с моделью
 *  (packages/app/src/prefs/themes.coverage.test.ts).
 *
 *  Файл существует, чтобы ~45 потребителей `from "../types"` не получили дифф
 *  в этапе, который к ним отношения не имеет (паттерн Э0, см. i18n). Новый код
 *  импортирует из "@muza/app/prefs/types" напрямую.
 *
 *  `View` НЕ переехал и живёт здесь по-настоящему: это роутинг оболочки
 *  ПРИЛОЖЕНИЯ (один экран = один ключ состояния). У веба маршруты Next-роутера
 *  и своих экранов вроде мини-плеера нет — общего смысла у типа не возникает,
 *  а в @muza/app он потянул бы за собой чужие знания об окнах Tauri. */

export * from "@muza/app/prefs/types";

export type View =
  | "home"
  | "search"
  | "favorites"
  | "library"
  | "stats"
  | "playlist"
  // read-only страница плейлиста SoundCloud из выдачи (2026-07-20)
  | "scPlaylist"
  | "settings"
  | "admin";
