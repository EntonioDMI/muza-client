/** ВИЛКА БРАУЗЕРА (Э2 веб-паритета, 2026-08-02): что умеет площадка «веб».
 *  Розетка и правила — packages/app/src/platform/types.ts.
 *
 *  Тут почти пусто — и это НЕ заготовка, а сам ответ. Страница не умеет ни
 *  вынести файл на рабочий стол, ни узнать путь к файлу на диске, ни
 *  управлять окном. Поля просто нет: общий код увидит `platform.dragOut ===
 *  undefined` и не покажет ни жеста, ни пункта меню. Пустышек-заглушек,
 *  которые «вроде есть, но ничего не делают», здесь заводить НЕЛЬЗЯ — они
 *  дают пользователю кнопку, после нажатия на которую ничего не происходит.
 *
 *  Константа, а не фабрика (в отличие от приложения): у браузера умения не
 *  зависят от того, как его запустили, — проверять на старте нечего. */

import type { PlatformAdapter } from "@muza/app/platform";

export const webPlatform: PlatformAdapter = {
  system: {
    // Единственное, что площадки умеют ОБЕ. noopener обязателен: без него
    // открытая вкладка получает доступ к window.opener нашей страницы.
    openExternal: async (url) => {
      window.open(url, "_blank", "noopener");
    },
    // Запуска вместе с системой, значка у часов и прочего у вкладки нет —
    // методов нет тоже.
  },
  // dragOut отсутствует НАМЕРЕННО — см. шапку.
};
